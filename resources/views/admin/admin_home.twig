<!doctype html>
<html lang="el">
    <head>        
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="">
        <meta name="author" content="COMTECH">
        <link rel="icon" href=""> <!-- Add your ico -->
        <link rel="stylesheet" href="{{ base_url() }}/css/starter.css">
        <link rel="stylesheet" href="{{ base_url() }}/css/bootstrap.min.css">
        <link rel="stylesheet" href="{{ base_url() }}/css/signin.css" >
        <link rel="stylesheet" href="{{ base_url() }}/css/dashboard.css" >
        <link rel="stylesheet" href="{{ base_url() }}/css/w3.css" >
        <link rel="stylesheet" href="{{ base_url() }}/../vendor/fortawesome/font-awesome/css/font-awesome.min.css">
        <style>
            .inline {
                display: inline;
            }
            .link-button {
                background: none;
                border: none;
                cursor: pointer;
            }
            .link-button:focus {
                outline: none;
            }

        </style>
        {% if auth.check %}
        <title>{{ title[0][0] }} Διαχείρηση</title>
        {% else %}
        <title>Comtech Διαχείρηση</title>    
        {% endif %}
            
    </head>
    
    <body style="padding-top:40px;">
{% include 'admin/navbar.twig' %}

{% block contents %}
            <main role="main" class="col-md-9 ml-sm-auto col-lg-12 px-4 mt-3">
                <div class="text-center pt-3 pb-2 mb-3 border-bottom">
                    <h2>Καλώς ήρθατε στον πίνακα ελέγχου!</h2><br>
                    <p>Επιλέξτε από το μενού τη λειτουργία που θέλετε να πραγματοποιήσετε!</p>
                </div>
                <div class="text-center pt-3 pb-2 mb-3">
                    <strong><u>Τεχνική υποστήριξη</u></strong><br>
                    <div class="text-left col-2 offset-5">
                        <i class="fa fa-phone-square text-info"></i>&nbsp;23710 22378<br>
                        <i class="fa fa-envelope text-info"></i>&nbsp;<a href="mailto:support@e-comtech.gr">support@e-comtech.gr</a>
                    </div>
                    
                    
                </div>
            </main>
{% endblock %}
         
        <!-- Loading scripts -->    
        
        <script  type="text/javascript" src="{{ base_url() }}/js/jquery-3.3.1.slim.min.js"></script>
        
        <script  type="text/javascript" src="{{ base_url() }}/js/popper.min.js"></script>
    
        <script  type="text/javascript" src="{{ base_url() }}/js/bootstrap.min.js"></script>

        <script  type="text/javascript" src="{{ base_url() }}/js/main.js"></script>

        <!-- End of scripts -->

    </body>
</html>
